kind = "web"
previewPath = "/"
title = "املاک AI"
version = "1.0.0"
id = "artifacts/amlak-ai"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25452

[services.development]
run = "pnpm --filter @workspace/amlak-ai run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/amlak-ai", "run", "build" ]
publicDir = "artifacts/amlak-ai/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25452"
BASE_PATH = "/"
